server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

feign:
  circuitbreaker:
    # 在feign中开启断路器
    enabled: true
